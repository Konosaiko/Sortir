<form method="post" action="{{ path('app_sortie_create_delete', {'id': sortie.id}) }}" onsubmit="return confirm('Êtes vous sûr de vouloir supprimer la sortie ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sortie.id) }}">
    <button class="btn btn-danger">Supprimer la sortie</button>
</form>
